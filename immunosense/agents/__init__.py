"""Domain agents — biomarker, dietary, environment, wearable, symptoms_mood.

Each agent is a standalone module that processes one data modality.
Agents are coordinated by the Conductor (``immunosense.conductor``).
"""
